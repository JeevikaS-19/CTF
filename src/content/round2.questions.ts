/**
 * content/round2.questions.ts
 * AREA: Round 2
 * OWNER: TBD (Pair B)
 * STATUS: empty typed array — ready for content
 * TODO(content): add puzzle prompts/media/points (cipher, rebus, riddle, etc.)
 *
 * CRITICAL: This file must NEVER contain `answer` fields.
 * Answers live server-side only (Supabase `questions` table, not readable by anon).
 * See SCAFFOLD_SPEC.md section 4.
 */

import type { Question } from '../shared/types';

// Typed empty array — Pair B fills this with real puzzles
const round2Questions: Question[] = [];

export default round2Questions;
